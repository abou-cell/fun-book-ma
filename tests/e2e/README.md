# E2E readiness

Playwright is installed for deployment readiness.

To add end-to-end tests:

1. Create specs in `tests/e2e/*.spec.ts`.
2. Run with `npx playwright test` after adding a `playwright.config.ts`.
